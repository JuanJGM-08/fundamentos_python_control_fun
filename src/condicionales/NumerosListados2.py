dividendo = 10
divisor = 0
resultado = dividendo / divisor if divisor != 0 else "División por cero no permitida"
print(resultado)

# El programa define dos variables, 'dividendo' y 'divisor', y utiliza una expresión ternaria para evaluar si el divisor es diferente de cero antes de realizar la división. Si el divisor es cero, se asigna un mensaje indicando que la división por cero no está permitida. Finalmente, se imprime el resultado de la operación o el mensaje correspondiente.